import Popup from "./Popup.js";

class PopWithImages extends Popup {
  constructor() {}

  open({ name, link }) {
    super.open();
  }

  close() {
    super.close();
  }

  setEventListeners() {}
}
