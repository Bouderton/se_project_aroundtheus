import Popup from "./Popup.js";

export default class PopWithImages extends Popup {
  constructor() {}

  open({ name, link }) {
    super.open();
  }

  close() {
    super.close();
  }

  setEventListeners() {}
}
