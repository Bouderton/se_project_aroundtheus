export default class Popup {
  constructor(popupSelector) {
    this.popupSelector = popupSelector;
  }
  open(data) {}

  close(data) {}

  _handleEscClose() {
    //esc key event
  }

  setEventListeners() {
    //add click event
  }
}
